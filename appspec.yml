version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/app

hooks:
  BeforeInstall:
    - location: scripts/create_env.sh
      timeout: 300

  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 300