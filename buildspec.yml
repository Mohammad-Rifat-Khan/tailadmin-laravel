version: 0.2

phases:
  install:
    runtime-versions:
      docker: 20
  pre_build:
    commands:
      - echo "Starting build"
  build:
    commands:
      - docker build -t laravel-app .
  post_build:
    commands:
      - echo "Build done"